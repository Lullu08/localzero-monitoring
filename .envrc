# Use this project's asdf Python (see .tool-versions). Does not change other projects.
layout python "$HOME/.asdf/installs/python/3.11.0/bin/python"

PATH_add .scripts


# eval $(poetry env activate)
# corepack enable
# poetry sync after installing dependencies